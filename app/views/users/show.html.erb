<%= @user.email %>

<%= simple_form_for @address, url: "/ronin_address", :method => :patch  do |f| %>
  <%= f.input :address %>
  <%= f.input :user_id, :as => :hidden, :input_html => { :value => @user.id } %>
  <%= f.submit "Add Ronin Address", class: "btn btn-primary" %>
<% end %>
